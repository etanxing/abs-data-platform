{
  "name": "@abs/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist .wrangler"
  },
  "dependencies": {
    "@abs/data": "workspace:*",
    "@abs/pdf-generator": "workspace:*",
    "hono": "^4.7.4",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@abs/config": "workspace:*",
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.8.0"
  }
}
